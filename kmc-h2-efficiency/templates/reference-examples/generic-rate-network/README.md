# Generic KMC Rate-Network Example

This example is intentionally not H2-specific. It shows the minimum shape of a KMC event table and simulation-parameter file. Replace all events, state definitions, rates, and observables with user-approved system-specific content.

Special observables such as H2 formation efficiency should be implemented as custom output definitions or postprocessing scripts, not hard-coded as the only KMC use case.
